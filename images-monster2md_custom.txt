---
obsidianUIMode: preview
cssclasses:
- json5e-monster
{#if resource.tags }
tags:
{#for tag in resource.tags}
- {tag}
{/for}{/if}
statblock: inline
statblock-link: "#^statblock"
aliases:
{#each resource.aliases}
- {it}
{/each}
---
# {resource.name}
```statblock
{resource.5eStatblockYaml}
"relativepathlink": "[{resource.name}]({resource.vaultPath})"
```
^statblock


# {resource.name}
*Source: {resource.source}*
{#if resource.description }
{#if resource.hasImages }{resource.showPortraitImage}{/if}

{resource.description}
{#if resource.hasMoreImages }

{resource.showMoreImages}
{/if}
{#else if resource.hasImages}
{resource.showAllImages}
{/if}{#if resource.environment }

## Environment

{resource.environment}
{/if}
